<%= simple_form_for(@tipo_contrato, url: "#", method: "get", html: { disabled: true }) do |f| %>
<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
<%= render "shared/notice" %>
<div class="card">
  <div class="card-body">
    <%= render "tipo_contrato", f: f, disabled: true %>
  </div>
  <div class="card-footer">
    <%= link_to t("common.actions.edit"), edit_tipo_contrato_path(@tipo_contrato), class: "btn btn-secondary" %>
    <%= link_to t("common.back"), tipos_contratos_path, class: "btn btn-light" %>
  </div>
</div>
<% end %>

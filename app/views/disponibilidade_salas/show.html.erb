<%= simple_form_for(@disponibilidade_sala, url: "#", method: "get", html: { disabled: true }) do |f| %>
<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
<%= render "shared/notice" %>
<div class="card">
  <div class="card-body">
    <%= render "disponibilidade_sala", f: f, disabled: true %>
  </div>
  <div class="card-footer">
    <%= link_to t("common.actions.edit"), edit_disponibilidade_sala_path(@disponibilidade_sala), class: "btn btn-secondary" %>
    <%= link_to t("common.back"), disponibilidade_salas_path, class: "btn btn-light" %>
  </div>
</div>
<% end %>

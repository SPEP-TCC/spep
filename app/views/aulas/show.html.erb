<%= simple_form_for(@aula, url: "#", method: "get", html: { disabled: true }) do |f| %>
<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
<%= render "shared/notice" %>
<div class="card">
  <div class="card-body">
    <%= render "aula", f: f, disabled: true %>
  </div>
  <div class="card-footer">
    <%= link_to t("common.actions.edit"), edit_aula_path(@aula), class: "btn btn-secondary" %>
    <%= link_to t("common.back"), aulas_path, class: "btn btn-light" %>
  </div>
</div>
<% end %>
